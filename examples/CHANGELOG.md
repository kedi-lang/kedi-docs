# Example Project Changelog

## 2.0.0

- Added CSV exports for reports.
- Renamed `user_id` to `account_id` in API responses. The old field is removed.
- Existing integrations must read `account_id` instead of `user_id`.
- Authentication and request parameters are unchanged.

This fictional changelog accompanies the illustrative release_team.kedi example.
